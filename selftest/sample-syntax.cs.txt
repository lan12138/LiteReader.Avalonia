// LiteReader 着色 / 跨行状态 自检样本
// 作者：hujie  创建：2026-09-18
//
// 覆盖点：跨行块注释、块注释后续行恢复、行内注释、字符串里的注释符号、
//   制表符对齐、CJK 宽度、三引号跨行字符串、数字与关键字。

public static class Sample
{
    /* 这是一个跨行块注释 —— 从这一行开始
       public void ThisShouldBeComment() { return; }
       "引号" 'singles' /* 嵌套的星号斜杠不该提前结束 */
       上面这一行结束时就该闭合了 */

    public int Add(int a, int b)      // 行内注释：public int 不该被着色成关键字
    {
        string s = "字符串里的 /* 不是注释 */ 和 // 双斜杠也不是";
        return a + b;   // 0x1F、3.14、1_000_000 是数字
    }

	制表符	开头的行	用来验证制表位对齐
	第二个制表行	TabWidth 默认 4

    /* 未闭合的块注释开始
       后续所有行都应保持注释色，直到遇到结束符 */
    public string Unclosed = "这一行结尾的引号没有闭合
       所以下一行也被当成字符串着色 —— 这是刻意的续行语义
       直到这一行才闭合";
}
